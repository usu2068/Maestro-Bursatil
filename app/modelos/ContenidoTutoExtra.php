<?php

namespace App\modelos;

use Illuminate\Database\Eloquent\Model;


/**
 * Clase ContenidoTutoExtra
 * 
 * Esta clase representa un modelo Eloquent (ORM de Laravel).
 * 
 * Actualmente, no tiene configuraciones ni relaciones personalizadas.
 * Por defecto, Eloquent asociará esta clase con la tabla "contenido_tuto_extras" 
 * (nombre pluralizado automáticamente), a menos que se indique otro nombre de tabla explícitamente.
 * 
 * Puedes utilizar esta clase para representar registros de la tabla correspondiente,
 * y para agregar futuras relaciones o métodos relacionados con este tipo de contenido extra.
 */

class ContenidoTutoExtra extends Model
{
    //
}
